{
  "name": "brutils",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "vp fmt",
    "test": "vp lint && vp run test -r",
    "ready": "vp fmt && vp lint && vp run test -r && pnpm -r run build",
    "dev": "vp run @brutils/docs#dev",
    "prepare": "vp config"
  },
  "dependencies": {
    "typescript": "catalog:"
  },
  "devDependencies": {
    "vite": "workspace:*",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.13.1"
  },
  "packageManager": "pnpm@10.33.0"
}
