{
  "name": "brutils",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "vp fmt",
    "test": "vp lint && vp run test -r",
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run website#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite": "workspace:*",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.13.1"
  },
  "packageManager": "pnpm@10.33.0"
}
